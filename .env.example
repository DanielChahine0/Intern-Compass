# Environment Variables for Local Development
# IMPORTANT: This file contains secrets - never commit to git!

# ==================== BACKEND VARIABLES ====================

# Database Configuration (PostgreSQL)
# Get these from your PostgreSQL provider (Render, Neon, Supabase, etc.)
PGHOST=your-actual-database-host.com
PGDATABASE=intern_compass
PGUSER=your-actual-username
PGPASSWORD=your-actual-password
PGSSLMODE=require

# AI Service (Google Gemini)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-actual-gemini-api-key
GEN_MODEL=gemini-1.5-flash

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS (For local development)
CORS_ORIGIN=http://localhost:8080


# ==================== FRONTEND VARIABLES ====================

# API Configuration (For local development)
VITE_API_URL=http://localhost:3001/api

# Auth0 Configuration
# Get these from your Auth0 dashboard: https://manage.auth0.com
VITE_AUTH0_DOMAIN=your-actual-tenant.auth0.com
VITE_AUTH0_CLIENT_ID=your-actual-client-id
VITE_AUTH0_CALLBACK_URL=http://localhost:8080/auth/callback


# ==================== NOTES ====================
# This file is ignored by git (.gitignore)
# Replace all "your-actual-*" values with real credentials
# For production, set these in your deployment platform's dashboard
