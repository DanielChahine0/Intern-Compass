# Environment Variables Template for Deployment
# Copy this file and fill in your actual values

# ==================== BACKEND VARIABLES ====================

# Database Configuration (PostgreSQL)
PGHOST=your-database-host.render.com
PGDATABASE=intern_compass
PGUSER=intern_compass_user
PGPASSWORD=your-secure-database-password
PGSSLMODE=require

# AI Service (Google Gemini)
GEMINI_API_KEY=your-gemini-api-key-here
GEN_MODEL=gemini-1.5-flash

# Server Configuration
PORT=3001
NODE_ENV=production

# CORS (Comma-separated for multiple origins)
CORS_ORIGIN=https://your-frontend-url.com,https://your-frontend-url.onrender.com


# ==================== FRONTEND VARIABLES ====================

# API Configuration
VITE_API_URL=https://your-backend-url.com/api

# Auth0 Configuration
VITE_AUTH0_DOMAIN=your-tenant.auth0.com
VITE_AUTH0_CLIENT_ID=your-auth0-client-id
VITE_AUTH0_CALLBACK_URL=https://your-frontend-url.com/auth/callback


# ==================== DEPLOYMENT NOTES ====================

# Render:
# - Set these in Render Dashboard → Service → Environment
# - Database credentials auto-populated if using Render PostgreSQL
# - CORS_ORIGIN and VITE_API_URL can use Render's service references

# Vercel:
# - Set VITE_* variables in Vercel Dashboard → Settings → Environment Variables
# - Prefix all frontend vars with VITE_

# Railway:
# - Set in Railway Dashboard → Service → Variables
# - DATABASE_URL auto-provided by Railway PostgreSQL

# Netlify:
# - Set in Netlify Dashboard → Site Settings → Environment Variables
# - Prefix all frontend vars with VITE_


# ==================== SECURITY REMINDERS ====================

# ⚠️ NEVER commit this file with real values to git
# ⚠️ Use platform-specific secret management
# ⚠️ Rotate API keys regularly
# ⚠️ Use different Auth0 apps for dev/staging/prod
# ⚠️ Enable 2FA on all deployment platforms
